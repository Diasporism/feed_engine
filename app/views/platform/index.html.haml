
.row-fluid
  .span3

.row-fluid
  .span12
    .span1
      %table.side_menu
        %tr
          %td Inbox
        %tr
          %td Gmail
        %tr
          %td Facebook
        %tr
          %td Twitter

  -if @timeline
    .span10
      %table.main
        %tr.header
          %th Sender
          %th Message
          %th Date/Time
        - @timeline.each do |message|
          %tr
          - if message.kind_of?(Email)
            %td
              = message.subject
          - else
            %td
              = message[:user][:screen_name] 
              .br
              = image_tag message[:user][:profile_image_url_https]
            %td
              = auto_link(message[:text], :html => { :target => '_blank' })
            %td
              = message[:created_at]



- paginate @timeline

